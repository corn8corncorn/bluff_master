# 🌐 如何分享應用給其他人使用

## ⚠️ 重要說明

**`localhost:3000` 只能在您的電腦上訪問，無法分享給其他人！**

要讓其他人透過網路訪問，需要使用 **Cloudflare Tunnel** 建立一個公開的 URL。

---

## 🚀 快速步驟

### 步驟 1: 安裝 Cloudflare Tunnel（只需安裝一次）

**Windows（使用 winget）：**

```cmd
winget install --id Cloudflare.cloudflared
```

**或手動下載：**

- 訪問：https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
- 下載 Windows 版本並安裝

### 步驟 2: 確保後端和前端都在運行

✅ **後端**：已在運行（您已啟動 `start-jar.bat`）
✅ **前端**：需要啟動

### 步驟 3: 啟動前端服務

開啟**新的終端視窗**，執行：

```cmd
cd frontend
start.bat
```

等待前端在 `http://localhost:3000` 啟動。

### 步驟 4: 建立公開 URL

開啟**另一個終端視窗**，執行：

```cmd
start-demo.bat
```

或者直接執行：

```cmd
cloudflared tunnel --url http://localhost:3000
```

### 步驟 5: 複製並分享 URL

Cloudflare Tunnel 啟動後，會顯示類似以下的 URL：

```
Your quick Tunnel has been created! Visit it at (it may take some time to be reachable):
https://type-barry-treatment-quantities.trycloudflare.com
```

**將這個 URL 複製並分享給其他人**，他們就可以在任何設備上訪問您的 DEMO！

---

## 📋 完整啟動流程（推薦）

### 方法 1: 使用完整啟動腳本（一鍵啟動）

```cmd
start-demo-full.bat
```

這個腳本會自動：

1. ✅ 啟動後端服務
2. ✅ 啟動前端服務
3. ✅ 建立 Cloudflare Tunnel
4. ✅ 顯示公開 URL

**適用情況：** 後端和前端都還沒啟動

### 方法 2: 手動啟動（您目前的情況）

因為您的後端已經在運行，使用這個方式：

1. **確認後端運行中**（已完成 ✅）
2. **啟動前端**：
   ```cmd
   cd frontend
   start.bat
   ```
3. **建立 Tunnel**：
   ```cmd
   start-demo.bat
   ```

---

## ⚠️ 注意事項

### 1. URL 會變化

- 每次啟動 Cloudflare Tunnel 時，URL 都會不同
- 如果 Tunnel 中斷，需要重新啟動並分享新的 URL

### 2. 保持服務運行

- 在 DEMO 期間，**不要關閉**後端、前端或 Tunnel 的終端視窗
- 關閉任一服務，其他人就無法訪問

### 3. 僅用於 DEMO

- Cloudflare Tunnel 適合用於展示和測試
- 生產環境請使用正式部署方案

### 4. WebSocket 支持

- Cloudflare Tunnel 自動支持 WebSocket 連接
- 遊戲的即時通訊功能可以正常使用

---

## 🆘 常見問題

### Q: 找不到 cloudflared 命令？

**解決方法：**

- 確認已正確安裝 Cloudflare Tunnel
- 重新開啟終端視窗
- 或使用完整路徑運行

### Q: URL 無法訪問？

**檢查清單：**

- ✅ 後端服務是否在運行？
- ✅ 前端服務是否在運行？
- ✅ Tunnel 是否正常啟動？
- ✅ 等待幾秒鐘讓 Tunnel 完全建立連接

### Q: 前端無法連接後端？

**解決方法：**

- 確認後端在 `http://localhost:8080` 運行
- 檢查後端日誌是否有錯誤
- 確認 `application-dev.yml` 配置正確

---

## 🎯 測試流程

1. **本地測試**：先確認 `http://localhost:3000` 可以正常訪問
2. **啟動 Tunnel**：建立公開 URL
3. **測試公開 URL**：用自己的手機或其他設備訪問生成的 URL
4. **分享給其他人**：確認可以訪問後，再分享 URL

---

## 📱 分享範例

當您獲得公開 URL 後，可以這樣分享：

```
📢 Bluff Master DEMO 連結：

https://xxxxx.trycloudflare.com

✨ 功能：
- 創建房間
- 多人即時遊戲
- 支援手機和電腦

⚠️ 注意：這是一個臨時連結，關閉服務後會失效
```

---

---

## 💻 在其他電腦啟動專案

### ⚠️ 重要：不只是安裝 Cloudflare、MySQL、Maven！

要在其他電腦啟動這個專案，您需要安裝以下**所有**軟體和進行配置：

### 📋 完整需求清單

#### 1. 必須安裝的軟體

- ✅ **Java 17+** - Spring Boot 運行環境

  - 下載：https://www.oracle.com/java/technologies/downloads/
  - 驗證：`java -version`

- ✅ **Maven 3.6+** - Java 專案建置工具

  - 下載：https://maven.apache.org/download.cgi
  - 驗證：`mvn -version`

- ✅ **Node.js 18+** - 前端運行環境

  - 下載：https://nodejs.org/
  - 驗證：`node -v` 和 `npm -v`

- ✅ **MySQL 8.0+** - 資料庫

  - 下載：https://dev.mysql.com/downloads/mysql/
  - 驗證：`mysql --version`

- ✅ **Redis 6.0+** - 快取和會話管理

  - Windows：https://github.com/microsoftarchive/redis/releases
  - macOS：`brew install redis`
  - Linux：`sudo apt-get install redis-server`
  - 驗證：`redis-cli ping`（應返回 `PONG`）

- ✅ **Cloudflare Tunnel**（可選，僅用於分享 DEMO）
  - Windows：`winget install --id Cloudflare.cloudflared`
  - 驗證：`cloudflared --version`

#### 2. 資料庫設定

1. **啟動 MySQL 服務**

   ```cmd
   # Windows
   net start MySQL80

   # macOS/Linux
   sudo systemctl start mysql
   ```

2. **建立資料庫**

   ```sql
   CREATE DATABASE bluff_master CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **設定資料庫帳號密碼**

   - 編輯 `backend/src/main/resources/application.yml`
   - 或設定環境變數：

     ```cmd
     # Windows PowerShell
     $env:DB_USERNAME="root"
     $env:DB_PASSWORD="your_password"

     # macOS/Linux
     export DB_USERNAME=root
     export DB_PASSWORD=your_password
     ```

#### 3. 啟動 Redis

```cmd
# Windows
redis-server

# macOS
brew services start redis

# Linux
sudo systemctl start redis
```

#### 4. 後端設定與啟動

1. **進入後端目錄**

   ```cmd
   cd backend
   ```

2. **編譯專案**

   ```cmd
   mvn clean install
   ```

3. **啟動後端**

   ```cmd
   mvn spring-boot:run
   ```

   或使用腳本：

   ```cmd
   start.bat
   ```

   後端將在 `http://localhost:8080` 啟動

#### 5. 前端設定與啟動

1. **進入前端目錄**

   ```cmd
   cd frontend
   ```

2. **安裝依賴**（首次必須執行）

   ```cmd
   npm install
   ```

3. **啟動前端**

   ```cmd
   npm run dev
   ```

   或使用腳本：

   ```cmd
   start.bat
   ```

   前端將在 `http://localhost:3000` 啟動

### ✅ 完整啟動檢查清單

在另一台電腦上，請確認：

- [ ] Java 17+ 已安裝並可執行
- [ ] Maven 3.6+ 已安裝並可執行
- [ ] Node.js 18+ 已安裝並可執行
- [ ] MySQL 8.0+ 已安裝並運行中
- [ ] Redis 6.0+ 已安裝並運行中
- [ ] 資料庫 `bluff_master` 已建立
- [ ] 資料庫帳號密碼已正確配置
- [ ] 後端依賴已編譯（`mvn clean install`）
- [ ] 前端依賴已安裝（`npm install`）
- [ ] 後端服務已啟動（`http://localhost:8080`）
- [ ] 前端服務已啟動（`http://localhost:3000`）

### 🚀 快速啟動腳本

如果所有軟體都已安裝，可以使用：

```cmd
# 方法 1: 完整啟動（後端 + 前端 + Cloudflare Tunnel）
start-demo-full.bat

# 方法 2: 分別啟動
# 終端 1 - 後端
cd backend
start.bat

# 終端 2 - 前端
cd frontend
start.bat

# 終端 3 - Cloudflare Tunnel（可選）
start-demo.bat
```

### ⚠️ 常見問題

**Q: 為什麼需要 Redis？**

- Redis 用於會話管理和快取，WebSocket 功能需要它

**Q: 為什麼需要 Node.js？**

- 前端是 Vue.js 應用，需要 Node.js 來運行開發伺服器

**Q: 為什麼需要 Java？**

- 後端是 Spring Boot 應用，需要 Java 運行環境

**Q: GCP Storage 需要設定嗎？**

- 不需要！本地開發使用 `local` 模式，圖片會存在 `./uploads` 目錄

**Q: 資料庫密碼在哪裡設定？**

- 在 `backend/src/main/resources/application.yml` 中修改 `spring.datasource.password`
- 或使用環境變數 `DB_PASSWORD`

---

## 📚 更多資訊

詳細說明請查看 `DEMO_GUIDE.md` 和 `START.md` 文件。
